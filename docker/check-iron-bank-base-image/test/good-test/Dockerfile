# Use Iron Bank base image for Jenkins
FROM harbor.cloudiolite-demo.karthikconsulting.com/jenkins:2.59

# Example: Add additional configurations or dependencies if needed
# For demonstration purposes, we're adding an additional package
RUN apt-get update && apt-get install -y additional-package
